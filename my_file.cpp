#include<iostream>
#include<cmath>
float a,b,c,root1,root2,delta
cout <<"Enter three numbers a,b,c of"<<"ax2 + bx +c";
cin >> a >> b >> C
if(!a)  //if a is zero
cout<<"The value of a should not be zero" <<"\n Abosting!!!!!"<<"\n"
else
{
delta=b*b-4*a*c;  //begining of else's body
if(delta>0)
{
root1=(-b+ sqrt
